# CLAUDE.md

**STOP. Read [AGENTS.md](AGENTS.md) before you do anything else in this repo.**

All agent rules live in `AGENTS.md` and nowhere else. This file is only a pointer, so
that there is a single file to maintain instead of one per agent.

Do not act, answer, or edit anything before reading it. Every rule there is mandatory,
including the ones about response length, plain language, complete sentences, and never
making changes without asking first.

If you skipped it, stop and go read it now.
